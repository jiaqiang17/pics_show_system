## 1. 需求实现
- [x] 1.1 设计批量标签更新请求结构，补充 `server/model/request` 与参数校验
- [x] 1.2 在 `server/service` 中实现批量标签插入、删除逻辑并落库，同时写入操作日志
- [x] 1.3 新增或更新 `server/api/v1` 与 `router` 入口，接入权限控制、审计中间件
- [x] 1.4 扩展作品列表查询接口，增加多标签筛选与计数返回
- [x] 1.5 前端 `web/src/api` 新增批量标签接口封装，并在 `pinia` store 中处理状态刷新
- [x] 1.6 更新作品管理视图，支持批量选择、标签编辑弹窗、筛选表单
- [x] 1.7 更新展示页请求，确保标签筛选条件透传并正确渲染结果

## 2. 测试与验证
- [ ] 2.1 执行 `cd server && go test ./...`，确保服务逻辑覆盖新增路径（执行失败：插件 announcement/gen 编译错误需另行处理）
- [ ] 2.2 前端运行 `cd web && npm run lint`，解决所有静态检查错误（脚本缺失，未能执行）
- [ ] 2.3 在测试环境验证批量打标签、筛选、撤销功能流程并截图记录
